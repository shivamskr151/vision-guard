# Vision Guard — Environment variables
# Copy to .env and set values for your environment.

# API base URL (no trailing slash). Used when backend is available.
# Example: https://api.example.com/v1
VITE_API_BASE_URL=
